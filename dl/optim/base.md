# Optimizer Shared Utilities

Common helpers for all optimizers. Contains create_episode_list() for converting flat tensors to episode dicts using boundaries, save_checkpoint(), and load_checkpoint(). Used by sgd.py and ne/optim/*.py.
