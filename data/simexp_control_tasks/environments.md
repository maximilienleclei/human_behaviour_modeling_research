# SimExp Environment Configurations

ENV_CONFIGS dictionary mapping env names to data files and dimensions. Contains get_data_file() to retrieve subject-specific JSON filenames for CartPole, MountainCar, Acrobot, and LunarLander.
