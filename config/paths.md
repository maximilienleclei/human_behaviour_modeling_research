# Project Paths

Global directory paths used across the project.

## Purpose
Centralizes path configuration to avoid hardcoding paths throughout the codebase.

## Contents
- `PROJECT_ROOT` - Root directory of the repository
- `DATA_DIR` - Data directory (datasets, human data files)
- `RESULTS_DIR` - Results directory (model checkpoints, logs, plots)
