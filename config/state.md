# State Persistence Configuration

StatePersistenceConfig dataclass for continual learning: controls hidden state persistence (across generations/episodes) and transfer modes (env_transfer, mem_transfer, fit_transfer) used by evolutionary optimizers.
