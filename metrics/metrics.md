# Evaluation Metrics

Standardized metrics for model evaluation. Contains compute_cross_entropy() for loss and compute_macro_f1() for macro F1 with multiple sampling trials for stochastic policies. Used by dl/ and ne/ modules.
